<Window x:Class="LeagueOfNinjas.EditNinjaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeagueOfNinjas"
        mc:Ignorable="d"
        Name ="EditNinja"
        Title="EditNinjaWindow" Height="139.921" Width="300"
                DataContext="{Binding EditNinja, Source={StaticResource Locator}}">
    <StackPanel Height="270" VerticalAlignment="Top">
        <TextBlock>Name</TextBlock>
        <TextBox Text="{Binding SelectedNinja.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <TextBlock>Starting gold</TextBlock>
        <TextBox Text="{Binding SelectedNinja.Gold, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>

        <Button Command="{Binding UpdateCommand}" CommandParameter="{Binding ElementName=EditNinja}">Update</Button>
    </StackPanel>
</Window>
